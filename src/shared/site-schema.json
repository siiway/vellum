{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "site": {
      "$ref": "#/definitions/SiteConfig",
      "description": "Site-wide settings (brand, locales, landing page)."
    },
    "repos": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/RepoConfig"
      },
      "description": "Documentation sources rendered by this Vellum deployment."
    },
    "nav": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/NavItem"
      },
      "description": "Site-level top navigation. Each repo can also declare its own nav (vellum.json `nav` or VitePress themeConfig.nav) which takes precedence inside that repo."
    }
  },
  "required": [
    "site",
    "repos"
  ],
  "additionalProperties": false,
  "definitions": {
    "SiteConfig": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "Brand name shown in the NavBar and as the SSR <title> suffix."
        },
        "tagline": {
          "type": "string",
          "description": "Short subtitle shown on the landing page hero."
        },
        "logo": {
          "type": "string",
          "description": "URL of the site logo (SVG recommended).",
          "format": "uri-reference"
        },
        "favicon": {
          "type": "string",
          "description": "URL of the favicon.",
          "format": "uri-reference"
        },
        "themeColor": {
          "type": "string",
          "description": "<meta name=\"theme-color\"> value, e.g. \"#0078d4\".",
          "pattern": "^#([0-9a-fA-F]{3,4}|[0-9a-fA-F]{6}|[0-9a-fA-F]{8})$"
        },
        "footer": {
          "type": "string",
          "description": "Footer text rendered below every page."
        },
        "homepageRepo": {
          "type": "string",
          "description": "Slug of the repo whose root acts as the site's landing page. `/` redirects here, and the NavBar brand link points here with the current locale preserved."
        },
        "defaultLocale": {
          "type": "string",
          "description": "Locale code used when the URL has no locale prefix."
        },
        "locales": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/LocaleConfig"
          },
          "description": "All supported locales. Order is preserved in the language picker."
        },
        "socialLinks": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/SocialLink"
          },
          "description": "Icon-only links rendered in the NavBar between the locale picker and theme toggle. Built-in icons cover github / gitlab / x / discord / slack / mastodon / bluesky / youtube / linkedin / instagram / facebook / npm / rss / stackoverflow / reddit / twitch / telegram; custom glyphs go via `{ svg: \"<svg ...>\" }`."
        },
        "aiProviders": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/AiProvider"
          },
          "description": "Global AI provider pool. Defined once and consumed by every AI feature (aiSummary, aiChat, translate). Each entry's `id` is referenced by features via their `providers` filter. Order is failover order — the worker tries entries left-to-right and falls over to the next on HTTP 401/402/403/429/5xx or network failures. Required if any AI feature is configured."
        },
        "aiSummary": {
          "$ref": "#/definitions/AiSummaryConfig",
          "description": "Microsoft Learn-style \"AI Summary\" button rendered below the page title on doc pages. Reads from `site.aiProviders` — set `providers` here to narrow the pool. Credentials (API keys, Turnstile secret) live in worker env vars, not this file."
        },
        "aiChat": {
          "$ref": "#/definitions/AiChatConfig",
          "description": "\"Ask AI\" chat drawer. Floating button bottom-right opens a chat panel; the model can call docs tools (search_docs, fetch_page, list_repos, list_pages) to answer questions across the site. Reads from `site.aiProviders`; the agent loop locks to a single API shape (OpenAI vs Anthropic) based on the first provider in the resolved list, so mix shapes carefully."
        },
        "translate": {
          "$ref": "#/definitions/TranslateConfig",
          "description": "Machine translation. When configured, the worker fills in every locale listed in `targets` by running source markdown, sidebar labels, frontmatter strings, UI labels, and repo display strings through `site.aiProviders`. Translations are cached in the VELLUM_TRANSLATION_DB D1 binding and refreshed on webhook (per repo) or after `refreshDays` (hourly cron). Hand-translated files in a repo's locale subdir always win over the machine output."
        },
        "searchAliases": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "description": "Search synonyms. Each key is a term a reader might type; the value is the list of words the docs author probably used for the same concept. A reader searching for any of these terms also matches pages containing the others (alias hits score below primary hits). Merged on top of a built-in baseline (latex/math, auth/oauth, ws/websocket, …) so config only needs to spell out product-specific vocabulary."
        }
      },
      "required": [
        "title",
        "homepageRepo",
        "defaultLocale",
        "locales"
      ],
      "additionalProperties": false
    },
    "LocaleConfig": {
      "type": "object",
      "properties": {
        "code": {
          "type": "string",
          "description": "Locale code (e.g. \"en\", \"zh\", \"ja\"). Used in cache keys and i18n lookup."
        },
        "label": {
          "type": "string",
          "description": "Human-readable name shown in the language picker."
        },
        "prefix": {
          "type": "string",
          "description": "URL segment for this locale (e.g. \"zh\" → /repo/zh/...). Empty string means the locale lives at the root of each repo."
        },
        "machineTranslated": {
          "type": "boolean",
          "description": "Set automatically at config load when this locale comes from `site.translate.targets` rather than being declared explicitly. Authors should never set this by hand. UI uses it to badge translated pages and to skip the locale-subdir lookup."
        }
      },
      "required": [
        "code",
        "label",
        "prefix"
      ],
      "additionalProperties": false
    },
    "SocialLink": {
      "type": "object",
      "properties": {
        "icon": {
          "anyOf": [
            {
              "$ref": "#/definitions/SocialIconName"
            },
            {
              "type": "object",
              "properties": {
                "svg": {
                  "type": "string"
                }
              },
              "required": [
                "svg"
              ],
              "additionalProperties": false
            }
          ]
        },
        "link": {
          "type": "string"
        },
        "ariaLabel": {
          "type": "string"
        }
      },
      "required": [
        "icon",
        "link"
      ],
      "additionalProperties": false
    },
    "SocialIconName": {
      "type": "string",
      "enum": [
        "github",
        "gitlab",
        "x",
        "twitter",
        "discord",
        "slack",
        "mastodon",
        "bluesky",
        "youtube",
        "linkedin",
        "instagram",
        "facebook",
        "npm",
        "rss",
        "stackoverflow",
        "reddit",
        "twitch",
        "telegram"
      ]
    },
    "AiProvider": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Short identifier referenced by features via their `providers` filter. Must be unique within `site.aiProviders`.",
          "pattern": "^[a-z0-9][a-z0-9-]*$"
        },
        "provider": {
          "type": "string",
          "enum": [
            "workers-ai",
            "openai-compatible",
            "anthropic"
          ],
          "description": "\"workers-ai\" uses the env.AI binding (Cloudflare Workers AI; no API key needed). \"openai-compatible\" calls any OpenAI-shaped Chat Completions endpoint (OpenAI itself, OpenRouter, Together, Groq, llama.cpp, …). \"anthropic\" calls the Anthropic Messages API."
        },
        "model": {
          "type": "string",
          "description": "Default model id for this provider entry. Features may override via their own `model` field."
        },
        "baseUrl": {
          "type": "string",
          "description": "Base URL for openai-compatible providers, e.g. \"https://openrouter.ai/api/v1\". Ignored for workers-ai / anthropic.",
          "format": "uri-reference"
        },
        "apiKeyEnv": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ],
          "description": "Env var name that holds the API key for this provider — or an array of env var names when the same endpoint has multiple keys with independent quotas. As a shortcut, a single env var can also hold MULTIPLE keys separated by newlines (one per line); each line becomes its own failover attempt labelled `${envName}#N`. The two shapes stack — `apiKeyEnv: [\"FOO\", \"BAR\"]` with FOO holding two newline-separated keys yields three attempts (FOO#1, FOO#2, BAR). Defaults to \"VELLUM_AI_API_KEY\" when omitted."
        },
        "extraBody": {
          "type": "object",
          "additionalProperties": {},
          "description": "Arbitrary JSON merged into the provider request body before the worker's required fields (model / messages / stream / max_tokens / system, which always win). Use it to enable provider-specific features that don't have a first-class field on AiProvider — e.g. DeepSeek thinking mode (`{ \"chat_template_kwargs\": { \"enable_thinking\": true } }`), Anthropic extended thinking (`{ \"thinking\": { \"type\": \"enabled\", \"budget_tokens\": 4000 } }`), or extra sampling params (`{ \"top_p\": 0.95, \"presence_penalty\": 0.1 }`). Merged into env.AI.run input for workers-ai providers."
        }
      },
      "required": [
        "id",
        "provider"
      ],
      "additionalProperties": false
    },
    "AiSummaryConfig": {
      "type": "object",
      "properties": {
        "model": {
          "type": "string",
          "description": "Optional model override for every provider in the pool when this feature runs. When omitted, each provider's own `model` (or its built-in default) is used."
        },
        "providers": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional whitelist of provider ids from `site.aiProviders`. When omitted, every entry in the global pool is used (in declaration order — that's the failover order too). Use this to narrow the pool when a feature can only consume a subset."
        },
        "turnstileSiteKey": {
          "type": "string",
          "description": "Cloudflare Turnstile site key. When set, the AI Summary button mounts an invisible Turnstile widget and the worker verifies the token before calling the model. Pairs with the VELLUM_TURNSTILE_SECRET worker secret."
        },
        "cacheTtlSeconds": {
          "type": "number",
          "description": "How long to retain a generated summary in KV before regenerating it. Defaults to 604800 (7 days).",
          "minimum": 60
        }
      },
      "additionalProperties": false
    },
    "AiChatConfig": {
      "type": "object",
      "properties": {
        "model": {
          "type": "string",
          "description": "Optional model override applied to every provider in the pool when this feature runs. A stronger reasoning model is often worth the extra cost for chat answers."
        },
        "providers": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional whitelist of provider ids from `site.aiProviders`. The chat agent loop locks to a single API shape (Anthropic vs OpenAI-compatible) based on the first provider in the resolved list — list providers of the matching shape here when the global pool mixes both."
        },
        "turnstileSiteKey": {
          "type": "string",
          "description": "Cloudflare Turnstile site key. When set, the visitor solves one invisible challenge per chat session and the worker issues a 60-minute signed session token that subsequent messages present. Pairs with VELLUM_TURNSTILE_SECRET."
        },
        "maxIterations": {
          "type": "number",
          "description": "Maximum agent loop iterations (model-call → tool-call rounds) per user message. Defaults to 6.",
          "minimum": 1,
          "maximum": 12
        }
      },
      "additionalProperties": false
    },
    "TranslateConfig": {
      "type": "object",
      "properties": {
        "model": {
          "type": "string",
          "description": "Optional model override applied to every provider in the pool when translation runs. Translation is high-volume + batch-friendly + doesn't reward reasoning, so a cheap model usually wins."
        },
        "providers": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional whitelist of provider ids from `site.aiProviders`. When omitted, translation uses every provider in the global pool, in declaration order (the failover order)."
        },
        "targets": {
          "anyOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "const": "all"
            }
          ],
          "description": "Locale codes to auto-translate into. Pass an array of BCP47-style codes (e.g. `[\"es\", \"fr\", \"ja\", \"zh-CN\"]`) or the sentinel `\"all\"` to expand to every language in the IANA ISO 639-1 registry (~180 bare-language codes, sourced via the `iso-639-1` npm package). Region-coded variants like `zh-CN`/`pt-BR` aren't in the bare ISO 639-1 base set — list them explicitly. Each resolved code is merged into `site.locales` at load time with machineTranslated:true and a label resolved via ISO 639-1 / Intl.DisplayNames. Codes already declared in `site.locales` are skipped."
        },
        "refreshDays": {
          "type": "number",
          "description": "How long a cached translation row is considered fresh. Defaults to 5. The hourly cron tick re-runs the provider on rows older than this. Webhooks bust rows immediately on push, so this is the background-drift interval.",
          "minimum": 1,
          "maximum": 365
        },
        "concurrency": {
          "type": "number",
          "description": "Max in-flight translation calls per refresh tick. Defaults to 4. Tune against provider rate limits.",
          "minimum": 1,
          "maximum": 32
        },
        "batchSize": {
          "type": "number",
          "description": "Per-tick row budget for the scheduled refresher. Defaults to 50. Caps CPU + outbound-fetch time per cron invocation.",
          "minimum": 1,
          "maximum": 500
        }
      },
      "required": [
        "targets"
      ],
      "additionalProperties": false
    },
    "RepoConfig": {
      "type": "object",
      "properties": {
        "slug": {
          "type": "string",
          "description": "URL segment for the repo (e.g. \"prism\" → /prism/...). Must be unique within `repos[]`.",
          "pattern": "^[a-z0-9][a-z0-9-]*$"
        },
        "source": {
          "type": "string",
          "enum": [
            "github",
            "local"
          ],
          "description": "Where the markdown lives. \"github\" (default) reads from raw.githubusercontent.com; \"local\" reads from local-docs/{slug}/ bundled into the worker's ASSETS at build time.",
          "default": "github"
        },
        "owner": {
          "type": "string",
          "description": "GitHub owner. Required when source is \"github\"."
        },
        "repo": {
          "type": "string",
          "description": "GitHub repo name. Required when source is \"github\"."
        },
        "branch": {
          "type": "string",
          "description": "Default branch to fetch from. Used as the cache key suffix and as the fallback when no `versions[]` is declared."
        },
        "docsRoot": {
          "type": "string",
          "description": "Path inside the repo (or local source) to the docs tree, e.g. \"docs\". Empty string means the repo root is the docs root."
        },
        "displayName": {
          "type": "string",
          "description": "Human-readable name shown in the NavBar brand crumb and 404 suggestions."
        },
        "description": {
          "type": "string",
          "description": "Short tagline shown on the repo's home page and as the SSR meta description fallback."
        },
        "logo": {
          "type": "string",
          "description": "URL of the per-repo logo (SVG recommended).",
          "format": "uri-reference"
        },
        "editLinkPattern": {
          "type": "string",
          "description": "Template for the \"Edit this page\" link in the page footer. `:path` is substituted with the docs-root-relative path. Omit for local repos."
        },
        "versions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RepoVersion"
          },
          "description": "Optional version selector. Default version is used when no version is encoded in the URL."
        },
        "localPath": {
          "type": "string",
          "description": "Override for where local-source files live. Defaults to `local-docs/{slug}`. Path is relative to the project root."
        },
        "hideInBrand": {
          "type": "boolean",
          "description": "When true, the NavBar omits this repo's displayName crumb after the site title. Useful for the homepageRepo so the brand doesn't read \"Site | Site\"."
        },
        "excludeFromSearch": {
          "type": "boolean",
          "description": "When true, the repo is omitted from search results in both the per-repo dialog scope picker and the cross-repo full-page search. Use for landing-page repos and other \"system\" content the reader shouldn't be sent to by a query."
        },
        "socialLinks": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/SocialLink"
          },
          "description": "Per-repo override for site.socialLinks. Shown in the NavBar while the reader is inside this repo. The repo's own vellum.json#socialLinks or VitePress themeConfig.socialLinks take higher priority when present."
        },
        "searchAliases": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "description": "Per-repo search synonyms. Layered on top of the site-level alias map (and the built-in baseline) when searching within this repo. Use for vocabulary that only makes sense for this product — e.g. mapping a code-name to its public-facing term."
        }
      },
      "required": [
        "slug",
        "docsRoot",
        "displayName"
      ],
      "additionalProperties": false,
      "allOf": [
        {
          "if": {
            "anyOf": [
              {
                "not": {
                  "required": [
                    "source"
                  ]
                }
              },
              {
                "properties": {
                  "source": {
                    "const": "github"
                  }
                }
              }
            ]
          },
          "then": {
            "required": [
              "owner",
              "repo",
              "branch"
            ]
          }
        }
      ]
    },
    "RepoVersion": {
      "type": "object",
      "properties": {
        "label": {
          "type": "string",
          "description": "Human-readable label shown in the version picker (e.g. \"v2\", \"main\", \"next\")."
        },
        "branch": {
          "type": "string",
          "description": "Git branch / ref this version corresponds to."
        },
        "default": {
          "type": "boolean",
          "description": "True for the version selected when no explicit version is encoded in the URL."
        }
      },
      "required": [
        "label",
        "branch"
      ],
      "additionalProperties": false
    },
    "NavItem": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "Label shown in the NavBar."
        },
        "link": {
          "type": "string",
          "description": "Destination URL or site-relative path. Omit when this item is a dropdown trigger (use `items`)."
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/NavItem"
          },
          "description": "Children — turns this nav entry into a dropdown menu."
        },
        "activeMatch": {
          "type": "string",
          "description": "Regex (against the current page's repo-relative path) that marks this entry as active. Lets a nav entry stay highlighted across a whole section.",
          "format": "regex"
        }
      },
      "required": [
        "text"
      ],
      "additionalProperties": false,
      "oneOf": [
        {
          "required": [
            "link"
          ]
        },
        {
          "required": [
            "items"
          ]
        }
      ]
    }
  },
  "$id": "https://siiway.org/vellum/site-schema.json",
  "title": "Vellum site config",
  "description": "Top-level config consumed by the Vellum worker. Generated from src/shared/types.ts by scripts/gen-site-schema.ts — edit the types or that script's OVERRIDES table, not this file."
}
